experiment:
  name: softmax_mix_k32
  experiments_file: experiments.yaml
  overrides: {}

dataset:
  backend: local_flickr8k
  local:
    root_dir: null
  hf:
    dataset_name: nlphuji/flickr8k
    train_split: train
    val_split: null

model:
  vision: null
  text: null

run:
  checkpoint_dir: null
  print_experiment_comparison: true
